// 프로그래머스 레벨 3 섬 연결하기.
// 탐욕적인 방법을 이용하여 그래프의 모든 정점을 최소 비용으로 연결하는 방법 => Kruskal 알고리즘 

// Kruskal 알고리즘을 사용하는 방법?
// 처음 시작하는 노드로 부터 하나의 간선을 선택해가면 된다.


#include <string>
#include <vector>

using namespace std;

bool compare(vector<int> a, vector<int> b){
    return a[2] < b[2];
}

int solution(int n, vector<vector<int>> costs) {
    int answer = 0;
    return answer;
}
