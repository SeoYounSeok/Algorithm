+ 반환형이 참조형(Reference Type)인 경우

int& RefRetFuncOne(int &ref)
{
	ref++;
	return ref;
}

vs

int RefRetFuncTwo(int &ref)
{
	ref++;
	return ref;
}


참조 값을 받아서 하는 경우랑 값의 변화를 알 수 있었다.

int num2 = RefRetFuncOne(num1) (o)
int &num2 = RefRetFuncOne(num1) (o)
  
int num2 = RefRetFuncTwo(num1) (o)
int &num2 = RefRetFuncTwo(num1) (x)



+ 문자열 자르기
char str1[] = "a b c d e f";
char* tok1 = strtok(str1," ")
- 공백을 기준으로 문자열을 잘라서 나타낼 수 있습니다.

while(tok1 != NULL){
	cout<<tok1<<endl;
	tok1 = strtok(NULL," ");
}
